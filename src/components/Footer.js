import styles from "../styles/Footer.module.css"

const Footer = () => {

    return (
        <footer>
            <p className={styles.footer}>MMO HYPE <span>&copy;</span></p>
        </footer>
    );
}

export default Footer;